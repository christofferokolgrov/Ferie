{
  "name": "ferie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal monitor for restplasser (last-minute package-holiday seats) departing OSL.",
  "scripts": {
    "test": "node --test",
    "sweep:apollo": "node src/apollo.mjs"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
